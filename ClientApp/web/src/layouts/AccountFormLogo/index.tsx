import React from "react";

import Logo from "@/layouts/Logo";

const AccountFormLogo = () => (
  <Logo mode="horizontal" theme="light" style={{ width: "unset", textAlign: "center", float: "none", transform: "translateY(-100%) scale(2)" }} />
);

export default AccountFormLogo;
